import { useState } from "react";
import { useContext } from "react";
import { AppContext } from "../../context/AppContext";
import "./AuthPage.scss";

export function AuthPage() {
  const { dispatch } = useContext(AppContext);
  const [mode, setMode] = useState("login");
  const [form, setForm] = useState({
    name: "",
    email: "",
    password: "",
    confirm: "",
  });
  const [loading, setLoading] = useState(false);
  const nav = (p) => {
    dispatch({ type: "SET_PAGE", page: p });
    window.scrollTo(0, 0);
  };

  function handleAuth() {
    if (!form.email || !form.password) {
      dispatch({
        type: "ADD_TOAST",
        toast: {
          type: "error",
          title: "Thiáº¿u thÃ´ng tin",
          msg: "Vui lÃ²ng Ä‘iá»n email vÃ  máº­t kháº©u",
        },
      });
      return;
    }
    setLoading(true);
    setTimeout(() => {
      const user = {
        name:
          mode === "register"
            ? form.name || form.email.split("@")[0]
            : form.email === "admin@techzone.vn"
              ? "Admin TechZone"
              : form.email.split("@")[0],
        email: form.email,
        role: form.email === "admin@techzone.vn" ? "ADMIN" : "USER",
        avatar: null,
      };
      dispatch({ type: "SET_USER", user });
      dispatch({
        type: "ADD_TOAST",
        toast: {
          type: "success",
          title:
            mode === "login"
              ? "ÄÄƒng nháº­p thÃ nh cÃ´ng!"
              : "Táº¡o tÃ i khoáº£n thÃ nh cÃ´ng!",
          msg: `ChÃ o má»«ng, ${user.name}!`,
        },
      });
      nav("home");
      setLoading(false);
    }, 800);
  }

  function googleLogin() {
    setLoading(true);
    setTimeout(() => {
      const user = {
        name: "Nguyá»…n Google User",
        email: "google.user@gmail.com",
        role: "USER",
        avatar: null,
      };
      dispatch({ type: "SET_USER", user });
      dispatch({
        type: "ADD_TOAST",
        toast: {
          type: "success",
          title: "ÄÄƒng nháº­p Google thÃ nh cÃ´ng!",
          msg: `ChÃ o má»«ng, ${user.name}!`,
        },
      });
      nav("home");
      setLoading(false);
    }, 600);
  }

  return (
    <div className="auth-wrapper" style={{ paddingTop: 64 }}>
      <div className="auth-bg" />
      <div className="auth-card">
        <div className="auth-logo">
          <div className="auth-logo-text">TechZone</div>
          <div style={{ fontSize: 12, color: "var(--muted)", marginTop: 4 }}>
            Your Tech Universe
          </div>
        </div>
        <h2 className="auth-title">
          {mode === "login" ? "ÄÄƒng nháº­p" : "Táº¡o tÃ i khoáº£n"}
        </h2>
        <p className="auth-sub">
          {mode === "login"
            ? "ChÃ o má»«ng trá»Ÿ láº¡i! ÄÄƒng nháº­p Ä‘á»ƒ tiáº¿p tá»¥c."
            : "Táº¡o tÃ i khoáº£n Ä‘á»ƒ mua sáº¯m dá»… dÃ ng hÆ¡n."}
        </p>

        <button className="google-btn" onClick={googleLogin}>
          <svg width="18" height="18" viewBox="0 0 18 18">
            <path
              d="M17.64 9.2c0-.637-.057-1.251-.164-1.84H9v3.481h4.844c-.209 1.125-.843 2.078-1.796 2.717v2.258h2.908c1.702-1.567 2.684-3.875 2.684-6.615z"
              fill="#4285F4"
            />
            <path
              d="M9 18c2.43 0 4.467-.806 5.956-2.184l-2.908-2.258c-.806.54-1.837.86-3.048.86-2.344 0-4.328-1.584-5.036-3.711H.957v2.332C2.438 15.983 5.482 18 9 18z"
              fill="#34A853"
            />
            <path
              d="M3.964 10.707c-.18-.54-.282-1.117-.282-1.707s.102-1.167.282-1.707V4.961H.957C.347 6.175 0 7.548 0 9s.348 2.825.957 4.039l3.007-2.332z"
              fill="#FBBC05"
            />
            <path
              d="M9 3.58c1.321 0 2.508.454 3.44 1.345l2.582-2.58C13.463.891 11.426 0 9 0 5.482 0 2.438 2.017.957 4.961L3.964 7.293C4.672 5.163 6.656 3.58 9 3.58z"
              fill="#EA4335"
            />
          </svg>
          Tiáº¿p tá»¥c vá»›i Google
        </button>

        <div className="divider">hoáº·c</div>

        {mode === "register" && (
          <div className="form-group" style={{ marginBottom: 14 }}>
            <input
              className="input"
              placeholder="Há» vÃ  tÃªn"
              value={form.name}
              onChange={(e) => setForm({ ...form, name: e.target.value })}
            />
          </div>
        )}
        <div className="form-group" style={{ marginBottom: 14 }}>
          <input
            className="input"
            type="email"
            placeholder="Email"
            value={form.email}
            onChange={(e) => setForm({ ...form, email: e.target.value })}
            onKeyDown={(e) => e.key === "Enter" && handleAuth()}
          />
        </div>
        <div
          className="form-group"
          style={{ marginBottom: mode === "register" ? 14 : 20 }}
        >
          <input
            className="input"
            type="password"
            placeholder="Máº­t kháº©u"
            value={form.password}
            onChange={(e) => setForm({ ...form, password: e.target.value })}
            onKeyDown={(e) => e.key === "Enter" && handleAuth()}
          />
        </div>
        {mode === "register" && (
          <div className="form-group" style={{ marginBottom: 20 }}>
            <input
              className="input"
              type="password"
              placeholder="XÃ¡c nháº­n máº­t kháº©u"
              value={form.confirm}
              onChange={(e) => setForm({ ...form, confirm: e.target.value })}
            />
          </div>
        )}

        {mode === "login" && (
          <div style={{ textAlign: "right", marginBottom: 16, fontSize: 13 }}>
            <span className="auth-link">QuÃªn máº­t kháº©u?</span>
          </div>
        )}

        <button
          className="btn btn-primary btn-lg w-full"
          style={{ width: "100%", marginBottom: 6 }}
          onClick={handleAuth}
          disabled={loading}
        >
          {loading
            ? "â³ Äang xá»­ lÃ½..."
            : mode === "login"
              ? "ðŸ”‘ ÄÄƒng nháº­p"
              : "ðŸš€ Táº¡o tÃ i khoáº£n"}
        </button>

        {mode === "login" && (
          <div
            style={{
              fontSize: 12,
              color: "var(--muted)",
              textAlign: "center",
              marginTop: 10,
              padding: "8px",
              background: "var(--card2)",
              borderRadius: 6,
            }}
          >
            ðŸ’¡ Demo: dÃ¹ng{" "}
            <strong style={{ color: "var(--accent)" }}>
              admin@techzone.vn
            </strong>{" "}
            Ä‘á»ƒ vÃ o Admin Panel
          </div>
        )}

        <div className="auth-footer">
          {mode === "login" ? (
            <>
              ChÆ°a cÃ³ tÃ i khoáº£n?{" "}
              <span className="auth-link" onClick={() => setMode("register")}>
                ÄÄƒng kÃ½ ngay
              </span>
            </>
          ) : (
            <>
              ÄÃ£ cÃ³ tÃ i khoáº£n?{" "}
              <span className="auth-link" onClick={() => setMode("login")}>
                ÄÄƒng nháº­p
              </span>
            </>
          )}
        </div>
      </div>
    </div>
  );
}

